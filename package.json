{
  "name": "hiltron-restarter",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "@playwright/test": "^1.54.1",
    "@types/node": "^24.1.0"
  },
  "scripts": {
    "dev": "playwright test --ui",
    "test:e2e": "playwright test",
    "test:e2e:ci": "playwright test --workers=4 --retries=2"
  },
  "dependencies": {
    "dotenv": "^17.2.1"
  }
}
